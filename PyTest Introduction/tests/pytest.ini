[pytest]
markers =
    validate_csv: Marks tests related to CSV content validation.
    unmarked: Mark for tests that were not explicitly marked.

testpaths = tests

python_files = test_*.py

addopts = --html=pytest_report.html --self-contained-html